<h1> <%= @doctor.name %></h1>
Department: <%= @doctor.department  %><br><br><br>
    <% @doctor.appointments.each do |appt| %>
    Appointment Time: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")  %><br>
    Patient Name: <%= link_to appt.patient.name, patient_path(appt.patient) %><br><br><br>
    <% end %>